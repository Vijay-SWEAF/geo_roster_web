Options -Indexes
DirectoryIndex index.php

RewriteEngine On
RewriteBase /geo-roster/

# Allow existing files and folders
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]