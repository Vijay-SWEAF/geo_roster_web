import 'package:flutter/material.dart';

/// OurNative colour palette — warm, nostalgic, elder-friendly
class AppColors {
  AppColors._();

  // ── Primary ──────────────────────────────────────────────
  static const Color primaryGreen   = Color(0xFF2D6A4F); // deep earthy green
  static const Color primaryBrown   = Color(0xFF6B4226); // warm brown
  static const Color primaryGold    = Color(0xFFB8860B); // muted saffron/gold

  // ── Background ───────────────────────────────────────────
  static const Color backgroundIvory = Color(0xFFFDF6EC); // warm ivory
  static const Color backgroundBeige = Color(0xFFF5EDD8); // light beige

  // ── Surface ──────────────────────────────────────────────
  static const Color surface        = Color(0xFFFFFBF0);
  static const Color surfaceCard    = Color(0xFFFFF8EE);

  // ── Accent ───────────────────────────────────────────────
  static const Color accentTerracotta = Color(0xFFCC5500); // terracotta
  static const Color accentMaroon     = Color(0xFF800020); // soft maroon
  static const Color accentGold       = Color(0xFFDAA520); // vintage gold

  // ── Text ─────────────────────────────────────────────────
  static const Color textPrimary    = Color(0xFF2C1810); // very dark brown
  static const Color textSecondary  = Color(0xFF6B4E3D); // medium brown
  static const Color textHint       = Color(0xFFAD8B73);
  static const Color textOnDark     = Color(0xFFFDF6EC);

  // ── Status ───────────────────────────────────────────────
  static const Color success        = Color(0xFF386641);
  static const Color warning        = Color(0xFFC77D17);
  static const Color error          = Color(0xFF9B2226);
  static const Color info           = Color(0xFF335C81);

  // ── Dividers / Borders ───────────────────────────────────
  static const Color divider        = Color(0xFFE8D5B7);
  static const Color border         = Color(0xFFD4B896);

  // ── Reaction colours ─────────────────────────────────────
  static const Color reactionRespect        = Color(0xFFDAA520);
  static const Color reactionBeautifully    = Color(0xFFE91E8C);
  static const Color reactionInspired       = Color(0xFF2196F3);
  static const Color reactionPrayers        = Color(0xFF9C27B0);
  static const Color reactionProud          = Color(0xFF4CAF50);
  static const Color reactionThankYou       = Color(0xFFFF6D00);

  // ── Role badge colours ───────────────────────────────────
  static const Color roleAdmin      = Color(0xFF9B2226);
  static const Color roleModerator  = Color(0xFF335C81);
  static const Color roleElder      = Color(0xFFB8860B);
  static const Color roleYouth      = Color(0xFF386641);
  static const Color roleMember     = Color(0xFF6B4226);
}
